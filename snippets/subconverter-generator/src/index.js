/* const get = require('lodash/get');

const { configs } = require('./constants');
const defaultConfigs = require('./defaultConfigs');
const { target, url, config: configPath } = require('./configs');

const config = get(configs, configPath);
 */
